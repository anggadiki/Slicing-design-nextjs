import SectionInfo from "../fragments/SectionContent";
import SectionMenu from "../fragments/SectionMenu";

const Section = () => {
  return (
    <>
      <SectionInfo />
      <SectionMenu />
    </>
  );
};

export default Section;
